!!! Thread-"Problem" loesen

!   Raum Modi anzeigen 
        - Benutzerlistenoperationen optimieren:
            - Voice/Operator-Status beachten (+nick/@nick)
            - Voice/Operator-Status-Aenderung anzeigen mit Nachricht

!   Mehrere Räume unterstützen
    add_message 
        muss raum beachten und in die variable schreiben und dann evtl.
        im aktuellen anzeigen falls die nachricht im aktuellen raum ist
    add_user
        muss name der benutzerliste übergeben bekommen
    delete_user
        muss in allen benutzerlisten nach dem benutzer suchen
        nicht wenn part/leave
    join 
        neuer eintrag in WxChoice_channel
        neue variablen(benutzerliste,eingabegeschichte,thema,ausgabe)
    leave/part
        raum aus WxChoices_channel entfernen
        in raum dadrunter gehen /anzeigen
    query/msg
        neuer eintrag in WxChoice_channel
        diesen anzeigen
    changenick und add_user und delete_user überarbeiten
    
    allgemein: virtuell jeweils einen buffer fuer einen channel halten

    1) neuer benutzerbefehl /join channelname, /part [channelname], alte
       channelspezifischen befehle umschreiben (weiterer parameter)
    2) ausgabefeld, benutzerliste in variable mitschreiben
    3) bei channelwechsel (durch dropdown, join, part) fensterinhalt mit
       entsprechendem variableninhalt ersetzen
    4) globale variable enthaelt aktuellen channelname (fuer send)
    --- ab hier unerheblich, trivial
    5) choicebox config parameter
    6) layout umstellung
    7) ping, highlights als notice bekanntgeben
    
!   Hinweise (nicht nachrichten) anders markieren und/oder
    andere Schrift / allgemein Uebersichtlichkeit

!   (Entfernte) Konfigurationsdatei:
        - Falls die lokale Konfigurationsdatei nicht vorhanden ist
          werden voreingestellte Werte verwendet und eine
          Konfigurationsdatei wird erstellt
        - Optionaler Eintrag in der lokalen Konfigurationsdatei, 
          welcher angibt, wo die Entfernte Konfigurationsdatei liegt
        - Die Entfernte Konfigurationsdatei ueberschreibt die lokalen
          Einstellungen
        - Muss nicht alle Optionen enthalten mit Ausnahme der
          IRC-Verbindungsdaten  
          
!   Falls die lokale Konfigurationsdatei nicht vorhanden ist
    werden voreingestellte Werte verwendet und eine
    Konfigurationsdatei wird erstellt.
    
!   Überprüfung ob die noetigen Verbindungsangaben in der
    Konfigurationsdatei eingetragen sind hinzufügen
    
!   Virus_!FEHL!_Alarm mit NOD32 "entfernen"

?   Verbindungsabbruch beim Beenden
?   Clientseitige Befehlsabfrage (/nick ... /join ... /quit ...)
?   Benutzerliste-buffer ist zu gross (scrollbar)     

OK  Sprachwahl fuer alle Texte (auch Themaleisten-Prefix,...) beachten:
OK   VERSION ignorieren und separat behandeln (CTCP)
OK  /msg-Befehl muss unterstuetzt werden
OK  Thema manuell abrufen bzw setzen mit "/topic [neuesthema]"
OK  Private nachrichten muessen gehen
OK  reset_nickname-Funktion soll aktuellen Nickname uebergeben
    bekommen um damit arbeiten zu koennen        
OK  keine leeren eingabefelder per button senden bestaetigen
OK  Pfeiltaste nach oben und history bitte
OK  Ops kommen ganz nach oben in der benutzerliste
OK  Standardtext (hinweis) in eingabefeld sollte man nicht senden
    koennen und diese auch verschwinden, wenn man reinklickt
    
OK  (fr) Gescheiter log (nur noetige angaben)
OK  (fr) keine 1sekuendige pause sondern nur 20 ms oder so
OK  (km) Folgende Raum Aktivitaeten noch beachten
       (kick)
       
